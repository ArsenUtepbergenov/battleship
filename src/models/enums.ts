export enum GameState {
  START,
  RUN,
  PLAY,
  OVER,
}

export enum Ships {
  TorpedoBoat = 1,
  Destroyer = 2,
  Cruiser = 3,
  Battleship = 4,
}

export enum Orientation {
  H = 'horizontal',
  V = 'vertical',
}
