export enum GameState {
  START,
  PLAY,
  OVER,
}

export enum Ships {
  TorpedoBoat = 1,
  Destroyer = 2,
  Cruiser = 3,
  Battleship = 4,
}

export enum Orientation {
  Horizontal = 'horizontal',
  Vertical = 'vertical',
}

export enum ColorType {
  DEFAULT,
  ERROR,
  SUCCESS,
  INFO,
}
